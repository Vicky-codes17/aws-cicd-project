version: 0.2

phases:

  install:
    commands:
      - echo Installing dependencies
      - pip3 install -r requirements.txt

  build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '**/*'